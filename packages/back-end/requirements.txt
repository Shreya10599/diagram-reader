fastapi==0.128.8
uvicorn[standard]
anthropic>=0.40.0
python-dotenv>=1.0.1
pydantic>=2.7

# Runtime: the extraction loop re-renders extracted data into a chart image
# (app/chart_render.py) so the verification pass can compare it against the original.
matplotlib>=3.8

# Eval-only — not used by the running app itself, just scripts/eval_accuracy.py
requests>=2.31
